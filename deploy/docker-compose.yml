services:
  chat-server:
    build:
      context: ../server
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    volumes:
      - chat-data:/data
    restart: unless-stopped

volumes:
  chat-data:
